import XCTest
@testable import swift_model_to_viewModel_mapper

final class swift_model_to_viewModel_mapperTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(swift_model_to_viewModel_mapper().text, "Hello, World!")
    }
}
